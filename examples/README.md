# examples/ — 金样（质量锚点）

精选示范 deck 集，**不按日期归档**（与 `decks/` 的边界：这里是 curated 质量基准，不是日常产物）。

## 作用

1. 新 deck 的质量对齐基准（few-shot 锚点：示范 > 描述）
2. 模板 / 主题 / pattern 改动后的回归基线——改动后逐页过一遍金样

## 准入标准

- 覆盖面：`demo` 必须覆盖 `patterns/` 全部模式
- 质量：通过 `docs/WORKFLOW.md` 完整 QA 清单 + 人工挑刺（问题记入 `docs/LESSONS.md`）
- 命名：语义名（`demo/`、`glow-showcase/`…），不用日期前缀

## 金样清单

| 金样 | 状态 | 说明 |
|------|------|------|
| `demo/` | 待建（P1） | 覆盖全部 patterns 的可视化文档 |
